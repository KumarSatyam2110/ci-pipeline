name: CI Pipeline
on:
        push:
                branches: 
                        - main
                        - develop
        pull_request:
                branches:
                        - main
                        - develop
jobs:
        ci-pipeline:
                runs-on: ubuntu-latest
                steps:
                        - name: Build
                          uses: actions/checkout@v3

                        - name: Print
                          run: echo "${{ github.ref }}"



